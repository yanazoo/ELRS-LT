# ARCHITECTURE: EP スニファーによるラップ検出

## システム全体図

```
 ┌──────────────────────────────────────────────────────────────────┐
 │  レーストラック                                                   │
 │                                                                   │
 │  [パイロット①TX]  [パイロット②TX]  [パイロット③TX]  [パイロット④TX]
 │        │                │                │                │      │
 │        └────────────────┴────────────────┴────────────────┘      │
 │                 ELRS uplink  2.4GHz LoRa FHSS（80ch）            │
 │                         ↓  ↓  ↓  ↓                              │
 │   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
 │   │EP1スニファ│  │EP1スニファ│  │EP1スニファ│  │EP1スニファ│        │
 │   │  #1      │  │  #2      │  │  #3      │  │  #4      │        │
 │   │ESP8285   │  │ESP8285   │  │ESP8285   │  │ESP8285   │        │
 │   │+SX1280   │  │+SX1280   │  │+SX1280   │  │+SX1280   │        │
 │   └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘        │
 │        └─────────────┴─────────────┴─────────────┘               │
 │                  ESP-NOW（2.4GHz, GateEP1Packet_t）               │
 │                              ↓                                    │
 │              ┌───────────────────────────┐                        │
 │              │  Gate Node（TTGO T8）      │                        │
 │              │  ESP32-WROVER-E           │                        │
 │              │  EMA + ラップ検出         │                        │
 │              └──────────┬────────────────┘                        │
 │                         │ UART（115200, JSON）                    │
 │              ┌──────────▼────────────────┐                        │
 │              │  Web Node（XIAO ESP32-S3）│                        │
 │              │  WiFi AP 20.0.0.1         │                        │
 │              └──────────┬────────────────┘                        │
 │                         │ WiFi                                    │
 │              ┌──────────▼────────────────┐                        │
 │              │  スマホ / PC ブラウザ      │                        │
 │              │  http://20.0.0.1          │                        │
 │              └───────────────────────────┘                        │
 └──────────────────────────────────────────────────────────────────┘
```

## 信号の流れ

```
パイロットTX --[LoRa downlink, FHSS]--> ドローンのEP (標準ELRS RX)
                                            |
                                            | テレメトリ uplink (同じFHSSシーケンス)
                                            v
                                ゲートEP スニファー x4 (カスタムファーム)
                                            |  スロットごとのRSSI
                                            |  ESP-NOW
                                            v
                                ゲートESP32 (TTGO T8, 既存ロジック)
                                            |  UART JSON (変更なし)
                                            v
                                Web Node (XIAO S3, 変更なし)
                                            |  WiFi AP
                                            v
                                       スマホ / PC UI
```

## なぜ downlink ではなく uplink なのか

- downlink（TX -> RX）はパイロットの据え置き送信機から発信される。その RSSI を
  ゲートで測ってもドローンの位置を追えない。
- uplink（RX -> TX のテレメトリ）はドローンの EP から発信される。ドローンが
  ゲートに近づくと、ゲートスニファーが測る uplink の RSSI がピークを迎える。
  そのピークが通過の瞬間。

## TRSS（テレメトリ）の切り分け方

ELRS 3.6.3 では OTA パケットの**下位2ビット**で種別が決まる:

| 種別 | bits | 発信元 |
|------|------|--------|
| RCDATA  | `0b00` | TX（据え置き） |
| MSPDATA | `0b01` | TX |
| SYNC    | `0b10` | TX |
| TLM     | `0b11` | **ドローン（テレメトリ）** |

スニファーは **種別 `0b11`（PACKET_TYPE_TLM）のパケットだけ** を計測対象にする。
これにより、据え置きの TX の信号レベルや移動に左右されず、ドローンの信号だけを
キャリブレーショングラフ・ラップ検出に反映できる。

### テレメトリ捕捉と平滑化（推奨比率 = 1:2）

**テレメトリ捕捉は比率で密度が決まる。** ゲートはチャンネルごとに 4 スロット滞在し、
ホップ時にリチューン（瞬間的に受信停止）する。

- **1:4 / 1:8 以上**: ドウェル内のテレメトリスロットが 1 個。それがリチューンと重なると
  取りこぼし、捕捉が疎（毎秒数発）になりグラフがギザつく。
- **1:2（推奨）**: ドウェルにテレメトリスロットが約 2 個入り、片方を取りこぼしても
  もう片方を確実に捕捉 → **毎秒 100 発超の密な捕捉**になり滑らか。

平滑化:

- 報告は**包絡線（エンベロープ）フィルタ**: テレメトリ受信で即上昇、間は `ENV_DECAY_DB`
  /50ms でゆるやかに減衰。疎な比率でも floor へ急落せず曲線になるが、根本的な滑らかさは
  密な捕捉（1:2）で得る。
- RSSI はテレメトリ（type `0b11`）のパケットでのみ読む（全パケットで読むとキャプチャ
  ループが重くなりテレメトリスロットを取りこぼすため）。
- SYNC は UID 照合・比率取得に加え、**fhssIndex+nonce でホップ位相を TX へ再同期**
  （`SYNC_PHASE_ALIGN`、索引ドリフト補正。`0` で無効化）。

実用上の推奨: **TX のテレメトリ比率を 1:2** にする（密に捕捉でき、固定距離で安定、
通過時も滑らかな山）。高比率（1:8〜1:128）は捕捉が疎でギザつくため非推奨。

## なぜ EP をゲート受信機に使うのか

EP1/EP2 TCXO = ESP8285（ESP8266相当MCU）+ SX1280（2.4GHz LoRa）+ TCXO。

- ESP8285 は Arduino + ESP-NOW に対応するため、各スニファーが Gate ESP32 と
  無線で通信できる。これで 420000baud の CRSF UART 4本が不要になる。
- SX1280 は EP の内部 SPI で ESP8285 に接続されており、カスタムファームから
  直接制御できる（周波数設定・RXモード・RSSI読み取り）。
- TCXO により発振が安定し、FHSS のタイミングドリフトが小さく同期しやすい。

## FHSS 追従

ELRS はホップシーケンスを 6バイトのバインド UID から決定論的に導出する:

```
seed = UID[2] | UID[3]<<8 | UID[4]<<16 | UID[5]<<24
hopSequence = shuffle(0..N-1, rng(seed))   // N ~= 80 ch (2.4GHz)
```

UID が分かれば、ゲートスニファーは全ホップを事前計算し、次スロットの SX1280
周波数を先回りでセットできる。難しいのは「時間的同期」、つまり今が何番目の
スロットかを知ること。これは下記のロックオンフェーズで解決する。

## ロックオンフェーズ（スニファーごと）

```
1. SCAN:    hopSequence を短い RX ドウェルで素早く順に切り替え、
            あるチャンネル index k でパケット（または強RSSI）を検出するまで回す。
2. ESTIMATE: 検出時刻 t0 + チャンネル k から現在のスロット index と、
            ELRS フレームクロックに対するタイマーオフセットを算出する。
3. FOLLOW:  スロットごとにホップ index を進める。連続ミスが続いたら SCAN に戻す。
```

TCXO のおかげで FOLLOW フェーズは粘り強い。ドローンが遠い（連続ミスが多い）
ときだけ再 SCAN すればよい。ラップ検出は信号が強いゲート近傍でのみ重要なので、
遠方での一時的なミスは許容できる。

## RSSI -> ラップ（既存プロジェクトを再利用）

スニファーは生 RSSI を Gate ESP32 に報告する。Gate ESP32 はそれを既存の
パイプラインへそのまま流す（変更なし）:

```
テレメトリRSSI (種別0b11) -> EMAフィルタ (alpha = 0.25) -> Enter/Exit 状態機械 -> sendLap()
```

状態機械（RotorHazard 方式）は `gate_node/main.cpp` に実装済み:

```
CLEAR --(ema > EnterAt)--> CROSSING
CROSSING: ピークとピーク時刻を追跡
CROSSING --(ema < ExitAt かつ クールダウン経過)--> sendLap(peakTime) -> CLEAR
```

デフォルト閾値: EnterAt -55dBm, ExitAt -62dBm, クールダウン 3000ms。
TX のバックグラウンド（-65〜-75dBm 程度）より十分高く設定し、登録直後に常時通過
状態へ入らないようにしている。これらは Calib タブからパイロットごとに調整可能。

## タイミング予算（250Hz の例）

```
ELRS スロット @ 250Hz     約 4.0 ms
SX1280 周波数切替         約 1.0 ms
RX ドウェル + RSSI読み    残り約 3 ms に収まる   -> OK
```

高パケットレート（500Hz, 約2ms）では余裕が縮むので、Step 4 で実測検証する。
テレメトリ Ratio が uplink の頻度を下げるため、ゲートでの実効サンプルレート =
パケットレート / テレメトリRatio。**実運用では TX 比率を 1:2** にして密な捕捉を確保する
（高比率はリチューン衝突で取りこぼし、捕捉が疎になる）。

## 識別モデルの変更

旧: パイロットを Aircraft Node のハードウェア MAC で識別。
新: パイロットを 6バイトの ELRS バインド UID で識別。

- ロースターは MAC の代わりに UID を保存（テキスト書式 `AA:BB:CC:DD:EE:FF` は同じ）。
- ゲートスニファーは追従すべき UID を持たされる。
- Gate ESP32 は受信 ESP-NOW パケットを送信元 MAC ではなく UID でキーにする。

## 障害モードと対策

| モード | 対策 |
|--------|------|
| ドローン遠方で同期ロスト | 許容。ミス連続で再SCAN |
| 2.4GHz 混雑（WiFi等） | TCXO + FHSS の狭帯域ドウェル、閾値調整 |
| テレメトリOFF（TXでRaceモード） | テレメトリ有効を必須化、または downlink追従にフォールバック |
| UI での UID 打ち間違い | 6バイトhex書式を検証、オンライン/オフライン状態を表示 |

---

## スロットタイミング図（250Hz 例）

```
  時間軸 ──────────────────────────────────────────────────────────▶

  ELRS TX:
  │← 4ms →│← 4ms →│← 4ms →│← 4ms →│
  ▓▓▓░░░░░▓▓▓░░░░░▓▓▓░░░░░▓▓▓░░░░░
   ch[k]   ch[k+1]  ch[k+2]  ch[k+3]   ← FHSS に従い毎スロット周波数変更

  スニファー FOLLOW モード:
  │◀1ms▶│◀──── RX ポーリング ────▶│
  ┌─────┬──────────────────────┬───┐
  │ 切替 │  sxPacketReceived()  │余白│  ← 1スロット = 4ms
  └─────┴──────────────────────┴───┘
   ↑                 ↑
   sxSetFrequencyHz  種別0b11(TLM)検出 → RSSI 取得（自由走行グリッド + SCAN 再ロック、SYNC 再アンカー無し）

  SCAN モード（ロックオン前）:
  │←1.5ms→│←1.5ms→│←1.5ms→│
  ▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒
  ch[0]   ch[1]   ch[2] …     ← 最初のヒットで FOLLOW へ
```

---

## デュアル無線版（EP1 Dual TCXO = ESP32-PICO-D4 + SX1280×2）

単機（ESP8285 + SX1280×1）版は、1基の無線で **FHSS追従** と **テレメトリ捕捉** を
時分割する。これがテレメトリ比率を上げられない根本原因だった：

- **リチューン盲点**: ホップごとに `SetStandby→SetRfFrequency→SetRx`（約0.5〜1ms）で
  受信不能になる。毎秒約4発しかない 1:128 では、その1発が盲点と重なると即欠落。
- **グリッド・ドリフト**: SYNC間はグリッドが自走するため、わずかなドリフトで
  「間違ったchに駐留」しテレメトリslotを丸ごと逃す。

**HappyModel EP1 Dual TCXO** はトゥルーダイバーシティ機で、ESP32-PICO-D4 に SX1280 が
2基ぶら下がる（SPIバス共有、NSS で個別選択）。この2基に役割を分担させる：

```
  時間軸 ─────────────────────────────────────────────────────────▶
  1ドウェル = FHSS_HOP_PERIOD_US (= ELRS_SLOT_US × FHSS_HOP_INTERVAL)

  Radio A (同期アンカー):
  │◀──── 現ch(chCur)でSYNC捕捉 ────▶│◀ 次ch先回り ▶│
  ┌──────────────────────────────────┬───────────────┐
  │  SYNC/UID/比率を固定・位相再同期  │ chNext を pre │
  └──────────────────────────────────┴───────────────┘
                                       ↑ 残り LEAD_AHEAD_US で chNext へ

  Radio B (テレメトリ計測):
  │◀──────────── 現ch(chCur) 全体 ────────────▶│◀ tail ▶│
  ┌────────────────────────────────────────────┬────────┐
  │  type 0b11 (TLM) の RSSI を計測             │ 境界跨 │
  └────────────────────────────────────────────┴────────┘
                                                 ↑ TAIL_LINGER_US だけ旧chに残る
  境界 ──────────────────────────────────────────┘ → Bは chNext へ合流
```

- **Radio A** がドウェル前半に現chでSYNCを拾い、`fhssIndex+nonce` で位相・hopIndex・
  テレメトリ比率を固定（`SYNC_PHASE_ALIGN`）。最終slotで次chへ先回りするので、
  境界時にAのリチューンは critical path に乗らない。SCAN・auto-discovery・再同期もAが担当。
- **Radio B** は現chにドウェル全体＋`TAIL_LINGER_US` だけ境界の先まで駐留し、
  **Aがリチューンで聞けない末尾のテレメトリslotをカバー**。RSSIはここで読む。
- ELRS ではダウンリンクとテレメトリ uplink は**同じch**なので、両者を現chに重ねて
  全slotを切れ目なくカバー＝**盲点ゼロ**。希少な 1:128 の1発も確実に捕捉できる。

### 位相再同期の注意（phaseAnchor）

SYNC からの位相再同期は、その無線が **現ch(chCur) に駐留中** に受けたSYNCのみで行う
（`handlePacket()` の `phaseAnchor`）。Aの先回り中／Bの tail linger 中に受けたSYNCは
別ドウェルに属し得るため、UIDスタンプと比率読み取りだけ行い、グリッド再同期はしない。

### 限界

デュアル化は **捕捉の確実性** を上げるが、**テレメトリの発生レート自体（=比率）** は変えられない。
1:128 @ 500Hz は 256ms間隔（毎秒約4サンプル）のまま。よって：

- ラップ**検出**は 1:128 でも安定して成立する（取りこぼしが消えるため）。
- ラップ**タイムの時間分解能**はサンプル間隔（最大256ms）が上限。精密計測には依然
  低比率（1:2〜1:8）が有利。

### ピン配置（Generic 2400 True Diversity PA / ESP32）

```
  共有SPI: SCK=GPIO25  MISO=GPIO33  MOSI=GPIO32
  Radio A: NSS=GPIO27  BUSY=GPIO36  DIO1=GPIO37  RST=GPIO26
  Radio B: NSS=GPIO13  BUSY=GPIO39  DIO1=GPIO34  RST=GPIO21
  LED:     GPIO16（要実機確認）
```

> GPIO36/39/34 は ESP32 の入力専用ピン（BUSY/DIO1 = 入力なので問題なし）。
> PA/RFスイッチ・LED・TCXO給電は基板依存のため実機で要検証。SX1280 は TCXO を外部給電
> する前提（既存単機ファーム同様 DIO3-TCXO制御はデフォルト無効、`SX_DIO3_TCXO_VOLTAGE`）。

実装: `src/gate_ep1_dual/`（`sx1280_radio.*` はインスタンス化したドライバ、`main.cpp` が
上記オーケストレーションを実装）。ESP-NOW のワイヤープロトコルは単機版と同一なので
`gate_node` / `web_node` は無改修。

---

## EP1/EP2 TCXO 内部ピン配置

ESP8285（MCU）と SX1280（2.4GHz LoRa）は基板上で以下の通り直結されています。  
外部パッドは書き込み・電源供給のみに使用します。

```
  EP1/EP2 TCXO 基板
  ┌──────────────────────────────────────┐
  │                                      │
  │  [SX1280]──SPI──[ESP8285]            │
  │                                      │
  │  SX1280 ピン    ESP8285 GPIO         │
  │  ──────────     ─────────────        │
  │  NSS (CS)   ←── GPIO15              │
  │  SCK        ←── GPIO14  (HSPI SCK)  │
  │  MOSI       ←── GPIO13  (HSPI MOSI) │
  │  MISO       ──► GPIO12  (HSPI MISO) │
  │  BUSY       ──► GPIO5               │
  │  DIO1 (IRQ) ──► GPIO4               │
  │  RST        ←── GPIO2               │
  │                                      │
  │  外部パッド:                          │
  │  ┌──────────────────────────────┐    │
  │  │ VCC │ GND │ TX(GPIO1) │ RX(GPIO3) │
  │  └──────────────────────────────┘    │
  │            ↑ ブートローダー兼用       │
  │                                      │
  │  LED ←── GPIO16                     │
  └──────────────────────────────────────┘
```

> RX パッド（GPIO3）は UART RX 兼ブートローダーエントリーピン。  
> 書き込み時は LOW に引いたまま VCC を投入する。
